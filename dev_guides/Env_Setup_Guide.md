# Installations

## Virtual Environment

```python
python -m venv venv
source venv/bin/activate   # On macOS/Linux
venv\Scripts\activate      # On Windows
pip install -r requirements.txt
```

## Project Structure

```python
project/
├── Lisn2MeAPI.py            # Main Flask app
├── requirements.txt         # List of dependencies
└── venv/                    # Virtual environment (optional)
```
